Leia `memory-bank/active-context.md` e `memory-bank/handoff.md`.

Mostre em uma tela, sem texto extra, as seguintes seções:

**Foco atual** — questão ou skill em andamento (de `active-context.md`)

**Pendências** — o que ficou aberto na última sessão (de `handoff.md`); se não houver, escreva "nenhuma"

**Bloqueios** — itens que impedem progresso (de `handoff.md`); se não houver, escreva "nenhum"

**Próxima ação** — sugestão de onde retomar (de `handoff.md`)

**Decisões aguardando validação** — se houver alguma em `active-context.md`

Não invente informações ausentes. Se um arquivo não existir ou estiver vazio, diga explicitamente.
