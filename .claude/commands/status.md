Leia `memory-bank/question-status.md` e `memory-bank/active-context.md`.

Mostre em uma tela, sem texto extra:

**Status das questões** — uma linha por questão no formato:
`Q{N} [{status}] {last_updated} — {nota}`

Use ícones simples para status: `✓` done · `→` in_progress · `○` not_started · `⚑` review

Ao final, adicione uma linha de resumo:
`{N} concluídas · {N} em progresso · {N} não iniciadas`

E uma linha com o foco atual extraída de `active-context.md`:
`Foco: {questão ou skill ativo}`

Não invente informações ausentes. Se um arquivo não existir, diga qual está faltando.
